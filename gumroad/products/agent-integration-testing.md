# Agent Integration Testing Skill

## Gumroad Title
Agent Integration Testing Skill

## Price
$3

## Short Description
Tests written and executed by agents. That actually work.

## Full Description

**Your agent writes integration tests in plain English, spawns subagents to execute them in isolation, and verifies every expectation with shell commands. No hallucinated test results — every pass or fail is backed by real output.**

### Who this is for
Developers building features who want automated integration testing. Teams using agent-driven development. Anyone who's caught their agent claiming a test passed when it didn't.

### What your agent learns
- Plain English test specs with clear repro steps
- Every expectation must be shell-verifiable
- One subagent per test for isolated execution
- Explicit prerequisites documentation
- Red flag detection for unverifiable tests
- Optional auto-fix via SWE/BUILDER subagents

### Compatibility
Works with any AI agent that reads markdown files: Claude Code, OpenCode, Cursor, Cline, Windsurf, Aider, and more.

### Format
SKILL.md — plain markdown, no dependencies, no lock-in. Drop into your project and your agent picks it up immediately.

---

*Zero hallucinated results — every assertion is backed by shell output*

*Part of the [Agentic Skills Collection](https://gumroad.com/l/agentic-skills-bundle) by [@av](https://github.com/av). Get all 10 skills for $10.*
