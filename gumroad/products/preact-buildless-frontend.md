# Buildless Frontend — Zero-Config Preact Skill

## Gumroad Title
Buildless Frontend — Zero-Config Preact Skill

## Price
$3

## Short Description
Frontends that run in the browser. No bundler. No build step.

## Full Description

**Your agent creates frontends that work immediately — ESM imports, CDN dependencies, hash routing, Preact signals. No Vite, no webpack, no 'npm install' that breaks. Ships with a starter template you can fork in seconds.**

### Who this is for
Developers building prototypes, tools, dashboards, or demos. Anyone who's lost hours to bundler configuration. People who want their frontend to work when you open the HTML file.

### What your agent learns
- Import maps for bare specifiers → CDN URLs
- CDN imports with pinned versions
- Cache-busting via versioned URLs or ETags
- Hash-based routing with Preact signals
- Lazy loading via dynamic `import()`
- JSDoc type safety without TypeScript build step
- CSP-compatible (nonce/hash for import maps)
- Starter template included

### Compatibility
Works with any AI agent that reads markdown files: Claude Code, OpenCode, Cursor, Cline, Windsurf, Aider, and more.

### Format
SKILL.md — plain markdown, no dependencies, no lock-in. Drop into your project and your agent picks it up immediately.

---

*Zero dependencies. Open HTML file → it works.*

*Part of the [Agentic Skills Collection](https://gumroad.com/l/agentic-skills-bundle) by [@av](https://github.com/av). Get all 10 skills for $10.*
