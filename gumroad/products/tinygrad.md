# Tinygrad — Deep Learning Framework Skill

## Gumroad Title
Tinygrad — Deep Learning Framework Skill

## Price
$3

## Short Description
A minimal tensor library with autograd, JIT, and multi-device support.

## Full Description

**Tinygrad is the deep learning framework for engineers who want to understand every line. This skill covers tensor ops, training loops, NN modules, optimizers, and the full UOp → Schedule → Codegen → Runtime pipeline. Your agent becomes a tinygrad contributor.**

### Who this is for
ML engineers and researchers. Developers contributing to tinygrad. Anyone who wants a deep learning framework they can actually read and modify.

### What your agent learns
- Tensor creation and lazy evaluation model
- Training loop pattern with TinyJit and @Tensor.train()
- NN modules: Linear, Conv2d, BatchNorm, LayerNorm, RMSNorm, Embedding, LSTMCell
- Optimizers: SGD, Adam, AdamW, LARS, LAMB, Muon
- UOp/PatternMatcher for graph transformations
- Architecture pipeline: Tensor → UOp → Schedule → Codegen → Runtime
- Debug env vars: DEBUG (1-7), VIZ, BEAM, NOOPT, SPEC

### Compatibility
Works with any AI agent that reads markdown files: Claude Code, OpenCode, Cursor, Cline, Windsurf, Aider, and more.

### Format
SKILL.md — plain markdown, no dependencies, no lock-in. Drop into your project and your agent picks it up immediately.

---

*Full architecture pipeline: Tensor to Runtime*

*Part of the [Agentic Skills Collection](https://gumroad.com/l/agentic-skills-bundle) by [@av](https://github.com/av). Get all 10 skills for $10.*
