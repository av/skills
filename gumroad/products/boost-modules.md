# Boost Modules — LLM Proxy Middleware Skill

## Gumroad Title
Boost Modules — LLM Proxy Middleware Skill

## Price
$3

## Short Description
Build custom middleware for your LLM pipeline.

## Full Description

**Harbor Boost is an optimizing LLM proxy. This skill teaches your agent to build custom Python modules that intercept and transform chat completions — reasoning chains, prompt injection, structured outputs, artifacts. Hot-reload workflow included.**

### Who this is for
Developers building LLM-powered products. Anyone who needs to intercept, transform, or augment LLM API calls. Teams building custom reasoning pipelines.

### What your agent learns
- Python module interface: `apply(chat, llm)` function
- Output methods: emit_message, emit_status, stream_chat_completion
- Chat tree manipulation (read, add, navigate conversation nodes)
- Pydantic structured output support
- Artifacts for UI clients (Open WebUI)
- Custom request parameters via `@boost_` prefix
- Hot-reload development workflow

### Compatibility
Works with any AI agent that reads markdown files: Claude Code, OpenCode, Cursor, Cline, Windsurf, Aider, and more.

### Format
SKILL.md — plain markdown, no dependencies, no lock-in. Drop into your project and your agent picks it up immediately.

---

*From echo to chain-of-thought in one `apply()` function*

*Part of the [Agentic Skills Collection](https://gumroad.com/l/agentic-skills-bundle) by [@av](https://github.com/av). Get all 10 skills for $10.*
