# Events — {{SLUG}}

Append-only. One line per transition:
`<epoch>  <from> --<event>--> <to>  visits=<n/budget|—>  effects=<short>`
Escalations and resumes are logged here too. Never rewrite history.

